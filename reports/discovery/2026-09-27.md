# GitHub作品発掘レポート — 2026-09-27

- 収集対象: owner一致のpublic 27件（通常 26件 / 明示許可fork 1件）
- 公開作品: 25件
- SEOアクション計画: metadata 0件 / README管理ブロック 0件 / 完全一致URL修正 0件
- ProtoPedia投稿候補: 0件（既存キュー。再生成と投稿は外側の固定スクリプトが担当）

## 新規公開作品

- なし。収集データに前回から追加されたpublicリポジトリはありません。

## 既存ページの事実更新

- `bakusoku-mahjong`、`ryokuru`、`sauna-clock`、`text-splitter-for-gpt`、`webgl2-gpu-benchmark`、`youtube-time-summary`: `repoUpdatedAt`を今回の公開GitHub収集値へ同期。
- GitHub URL、homepage / release URL、primary language、topics、`repoPushedAt`、archived状態は、ほかの既存GitHub作品も収集値および既存の公開根拠と一致していることを確認。
- 収集データには前回掲載済みリポジトリの欠落がなく、24件のREADMEキャッシュは収集時のSHAと一致しています。

## 既存本文の改善

- なし。大きな本文修正を要する根拠は見つからなかったため、既存の本人文体と内容固有の構成を維持しました。

## スキップした曖昧候補

- `HSBL_touka_Exhibition-plan`: READMEが題名だけで、作品としての用途と公開範囲を説明できないため見送り。
- `hsbl-github-catalog`: READMEキャッシュがなく、収集済みの説明だけでは独立した作品ページの本文を十分に組み立てられないため見送り。

## 除外

- `enso-WEBApp-BETA`: ポリシーで現行版 `enso` の旧版として指定されているため除外。
- `kicad1.6mm`: 空の公開リポジトリで、ポリシーの明示的除外にも該当するため除外。

明示許可forkの `easyeda2kicad-digimou` は、派生元と独自変更を記載した既存ページだけを維持し、GitHub SEO自動変更の対象外としています。ポリシーで一次情報を確認済みの外部作品 `aotori-575` と `katachi-tab` も既存掲載を維持しています。

## GitHub SEOアクション

- repository-metadata: なし
- readme-managed-intro: なし
- readme-exact-url-repair: なし

前回計画の対象 `HSBL-S800`、`ryokuru-WEBApp-BETA`、`Sauna-Clock-WEB-App`、`Text-splitter-for-GPT`、`WebGPU_Test`、`YouTube-Time-Summary` は、収集済みのDescription / Homepageが計画内容と一致しています。23件の既存GitHub作品を監査し、反映済みの6アクションを計画から除きました。

## テスト前の注意点

- 新規web-appはなく、新しいサムネイル撮影対象はありません。ProtoPediaキューと公開台帳は変更していません。
- 収集済み公開データとREADMEキャッシュだけを根拠にしています。外部URLの疎通とブラウザ上の表示は再検証していません。
- ネットワークを使う再収集、撮影、ProtoPediaへのログイン・投稿、リモートGitHubの更新、pushは外側の固定スクリプトが担当します。
- この環境では`tsx` CLIのIPCソケット作成が拒否されたため、監査・検証スクリプトは`node --import tsx`で同じ処理を実行しました。

## 検証結果

- Prettier: 変更した作品、監査、計画、発掘レポートを整形。収集JSONは収集スクリプトの出力形式を維持。
- content validation: 25作品すべて成功。
- SEOアクションのスキーマ・件数上限・収集データ照合: 0件で成功。通信を禁止したローカル適用dry-runも成功。
- ProtoPedia候補検証: 0件で成功。公開台帳とのローカル照合でも新規候補・未同期サムネイルは0件。
- `npm run typecheck`: エラー・警告とも0件。
- `npm run lint`: 成功。
- `npm run test`: 4ファイル・23テスト成功。
- Astro静的ビルドとbuild validation: HTML 36ファイル・公開作品25ルートで成功。
- `git diff --check`と差分確認: 成功。既存作品の変更は更新日時6行のみで、削除や本文変更はありません。
- systemdインストーラーの追加dry-run確認は、環境のソケット制限（`SO_PASSCRED: Operation not permitted`）で完了できずスキップ。サービス設定は変更していません。
